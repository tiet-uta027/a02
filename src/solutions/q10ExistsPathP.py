"""## Question 10 ##

Given an undirected graph $G(V,E)$ without
[self-loops](#loop), and a pair $(s,d)$ of source and
destination vertices, determine if a path between them
exists in the graph.

"""
from . import Graph,Bbt
def q10ExistsPathP(G:Graph,s:int,d:int) :
  """
Args:
  G: Graph resolves into `G.V` and `G.E`.
  s: Index of the source vertex.
  d: Index of the destination vertex.

"""

  pass
